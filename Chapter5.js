alert( 1 / 0 ); // Infinity
// Infinity is greater than any other value
alert( Infinity ); // Infinity

// NaN occurs as a result of incorrect type of information given
alert( "not a number" / 2 + 5 ); // NaN

// Use of Backtics `Hello`
let userName = 'John';
alert(`Hello, ${userName}`);
alert(`The result is ${4+4}`);

// Booleans
let hisResult = true;
let hisBadge = false;
alert(hisBadge);
alert(hisResult);

// They also come as a result of a comparison
let hisAge = 18 > 4;
alert(hisAge);

// Null states that variable is empty
let numb = null;

// undefined is like null
let x;
alert(x); // undefined

// typeof operator tells the type of the argument.
typeof("hello"); // stirng
typeof(12);
typeof(true);
typeof(fasle);
typeof(Math);
typeof(undefined);
typeof(null);


